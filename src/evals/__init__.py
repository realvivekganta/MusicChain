# --- Evaluation package ------------------------------------------------------
"""Evaluate the existing agent without adding a second application workflow.

The runner publishes isolated live trials; the graders score observed evidence using
pinned references. Importing the package alone performs no model or service calls.
"""
